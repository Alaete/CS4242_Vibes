1) Requirements : 
	- Need to have at least Python 3.6 

2) Extract the Vibes_Code.zip file and command prompt into it

3) Run "pip install -r requirements.txt" to install dependencies

4) Run "manage.py"